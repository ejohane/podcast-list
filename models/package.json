{
  "name": "podcast-list-models",
  "version": "1.0.0",
  "description": "Shared models between the frontend and backend",
  "main": "./dist/index.js",   
  "types": "./dist/index.d.ts",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  }
}
